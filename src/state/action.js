// src/redux/actions.js
// import { INCREMENT, DECREMENT } from './actionTypes';

//first page to be created is og action page.
export const increment = () => ({
  type: 'INCREMENT',
});

export const decrement = () => ({
  type: 'DECREMENT',
});
